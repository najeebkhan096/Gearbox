
class MyUser {
  final docid;
String ? uid;
  String ? imageurl;
  String ? email;
  String ? phone;
  String ? username;
  String ? carModal;
  String ? CarBrand;
  String ? device_id;


  DateTime ? date;

  MyUser({this.uid,this.email,this.username,this.imageurl,this.phone,this.docid,this.date,
    this.device_id,
    this.CarBrand,
    this.carModal

  });

}
MyUser ? currentUser;
