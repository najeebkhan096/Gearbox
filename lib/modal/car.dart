class Car{
  String ? title;
  String  ? modal;
  String ? description;
  String ? phone;
  String ? whatsno;
  double ? price;
  List<String> ? images;

Car({this.title,this.description,this.price,this.images,this.phone,this.whatsno,this.modal});
}
